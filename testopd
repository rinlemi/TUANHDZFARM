local player = game:GetService("Players").LocalPlayer
local pulling = false
local pullThread = nil
local selectedMob = nil

local gui = Instance.new("ScreenGui", game:GetService("CoreGui"))
gui.ResetOnSpawn = false

local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0, 220, 0, 320)
main.Position = UDim2.new(0, 10, 0.2, 0)
main.BackgroundColor3 = Color3.fromRGB(10, 10, 20)
main.BorderSizePixel = 0
main.Active = true
main.Draggable = true
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 12)
local stroke = Instance.new("UIStroke", main)
stroke.Color = Color3.fromRGB(255, 100, 0)
stroke.Thickness = 1.5

-- Title
local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1, 0, 0, 35)
title.BackgroundColor3 = Color3.fromRGB(180, 60, 0)
title.BorderSizePixel = 0
title.Text = "⚔️ Mob Puller"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 14
title.Font = Enum.Font.GothamBold
Instance.new("UICorner", title).CornerRadius = UDim.new(0, 12)

-- Refresh
local refreshBtn = Instance.new("TextButton", main)
refreshBtn.Size = UDim2.new(1, -10, 0, 28)
refreshBtn.Position = UDim2.new(0, 5, 0, 40)
refreshBtn.BackgroundColor3 = Color3.fromRGB(0, 100, 180)
refreshBtn.BorderSizePixel = 0
refreshBtn.Text = "🔄 Làm mới"
refreshBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
refreshBtn.TextSize = 12
refreshBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", refreshBtn).CornerRadius = UDim.new(0, 6)

-- Scroll list mob
local scroll = Instance.new("ScrollingFrame", main)
scroll.Size = UDim2.new(1, -10, 0, 160)
scroll.Position = UDim2.new(0, 5, 0, 73)
scroll.BackgroundColor3 = Color3.fromRGB(15, 15, 30)
scroll.BorderSizePixel = 0
scroll.ScrollBarThickness = 3
scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
Instance.new("UICorner", scroll).CornerRadius = UDim.new(0, 8)
local layout = Instance.new("UIListLayout", scroll)
layout.Padding = UDim.new(0, 3)
local pad = Instance.new("UIPadding", scroll)
pad.PaddingTop = UDim.new(0, 4)
pad.PaddingLeft = UDim.new(0, 4)
pad.PaddingRight = UDim.new(0, 4)

-- Status
local statusLabel = Instance.new("TextLabel", main)
statusLabel.Size = UDim2.new(1, -10, 0, 28)
statusLabel.Position = UDim2.new(0, 5, 0, 240)
statusLabel.BackgroundColor3 = Color3.fromRGB(20, 20, 40)
statusLabel.BorderSizePixel = 0
statusLabel.Text = "Chưa chọn mob"
statusLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
statusLabel.TextSize = 11
statusLabel.Font = Enum.Font.GothamBold
Instance.new("UICorner", statusLabel).CornerRadius = UDim.new(0, 6)

-- Pull/Stop button
local pullBtn = Instance.new("TextButton", main)
pullBtn.Size = UDim2.new(1, -10, 0, 40)
pullBtn.Position = UDim2.new(0, 5, 0, 273)
pullBtn.BackgroundColor3 = Color3.fromRGB(0, 140, 0)
pullBtn.BorderSizePixel = 0
pullBtn.Text = "🔴 Kéo Mob"
pullBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
pullBtn.TextSize = 14
pullBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", pullBtn).CornerRadius = UDim.new(0, 8)

local function refreshList()
	for _, v in ipairs(scroll:GetChildren()) do
		if v:IsA("TextButton") then v:Destroy() end
	end

	local seen = {}
	for _, mob in ipairs(workspace.Enemies:GetChildren()) do
		local hum = mob:FindFirstChild("Humanoid")
		if hum and hum.Health > 0 and not seen[mob.Name] then
			seen[mob.Name] = true

			local btn = Instance.new("TextButton", scroll)
			btn.Size = UDim2.new(1, 0, 0, 28)
			btn.BackgroundColor3 = Color3.fromRGB(25, 25, 45)
			btn.BorderSizePixel = 0
			btn.Text = "👾 " .. mob.Name
			btn.TextColor3 = Color3.fromRGB(220, 220, 220)
			btn.TextSize = 11
			btn.Font = Enum.Font.GothamBold
			btn.TextXAlignment = Enum.TextXAlignment.Left
			Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
			local p = Instance.new("UIPadding", btn)
			p.PaddingLeft = UDim.new(0, 8)

			btn.MouseButton1Click:Connect(function()
				selectedMob = mob.Name
				statusLabel.Text = "✅ " .. mob.Name
				statusLabel.TextColor3 = Color3.fromRGB(100, 255, 150)
				for _, b in ipairs(scroll:GetChildren()) do
					if b:IsA("TextButton") then
						b.BackgroundColor3 = Color3.fromRGB(25, 25, 45)
					end
				end
				btn.BackgroundColor3 = Color3.fromRGB(0, 80, 160)
			end)
		end
	end

	scroll.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 8)
end

local function stopPull()
	pulling = false
	if pullThread then task.cancel(pullThread) pullThread = nil end
	pullBtn.BackgroundColor3 = Color3.fromRGB(0, 140, 0)
	pullBtn.Text = "🔴 Kéo Mob"
	statusLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
end

local function startPull()
	if not selectedMob then
		statusLabel.Text = "❌ Chọn mob trước!"
		statusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
		return
	end
	pulling = true
	pullBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
	pullBtn.Text = "⏹ Dừng"

	pullThread = task.spawn(function()
		while pulling do
			local char = player.Character
			if not char then task.wait(0.1) continue end
			local hrp = char:FindFirstChild("HumanoidRootPart")
			if not hrp then task.wait(0.1) continue end
			for _, mob in ipairs(workspace.Enemies:GetChildren()) do
				if mob.Name ~= selectedMob then continue end
				local mobHrp = mob:FindFirstChild("HumanoidRootPart")
				local hum = mob:FindFirstChild("Humanoid")
				if not mobHrp or not hum or hum.Health <= 0 then continue end
				mobHrp.CFrame = hrp.CFrame
			end
			task.wait(0.05)
		end
	end)
end

pullBtn.MouseButton1Click:Connect(function()
	if pulling then stopPull() else startPull() end
end)

refreshBtn.MouseButton1Click:Connect(refreshList)

workspace.Enemies.ChildAdded:Connect(function()
	task.wait(0.5) refreshList()
end)
workspace.Enemies.ChildRemoved:Connect(function()
	task.wait(0.5) refreshList()
end)

refreshList()
print("✅ Mob Puller đã load!")
